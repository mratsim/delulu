[project]
name = "delulu-all-mcp-tests"
version = "0.1.0"
description = "MCP integration tests for delulu-all-mcp using the official MCP Python SDK"
requires-python = ">=3.12"
dependencies = ["mcp>=1.0,<2"]
